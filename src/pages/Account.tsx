const Account = () => {
  return (
    <main className="container mx-auto py-10">
      <h1 className="font-serif text-3xl md:text-4xl font-bold mb-6">Account</h1>
      <p className="text-muted-foreground">Sign in, orders, addresses, and saved payment methods will appear here once Supabase Auth is connected.</p>
    </main>
  );
};

export default Account;
